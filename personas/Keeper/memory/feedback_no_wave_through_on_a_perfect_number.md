---
name: feedback-no-wave-through-on-a-perfect-number
description: "A clean/perfect NUMBER doesn't bank a claim whose one load-bearing identification is assumed — necessary ≠ sufficient; localize the last step to a NAMED problem, don't bank on an assumption. The discipline holds HARDEST at the prettiest peak."
metadata:
  node_type: memory
  type: feedback
  originSessionId: 206b44d0-388f-4035-977c-310e7809ce46
---

**The muon arc, 2026-07-28 (exemplar).** The muon mass-ratio promotion to DERIVED came down to one gate (S2): does the condensate O project to a nonzero vector in the Jordan spin factor? Elie's sourced computation returned a *perfect* number — ⟨O,e⟩ = 0.000000, |v| = 1.000000 — and even closed the obvious skew worry (ℍ has no invariant vector). Everyone wanted to bank it. **I did not fire, and Cal's §125 audit found why: the number confirmed an *assumption*, not a derivation.** The clean |v|=1 relied on identifying the electroweak SU(2)²  with the SO(4) that fixes the Jordan identity e (**e = f**) — and that identification is genuinely unsourced (FG-2014 pins SO(5)×SO(2) content only; the SM gauge embedding is "one of BST's deepest open questions"). A *different* conjugate SO(4) (a "skew embedding") gives the same perfect number with a different meaning.

**The rule:** a perfect number verifies the *consistency* of the setup, not its *sourcing*. Before banking a Derived claim, isolate the ONE load-bearing identification and ask "is it derived, or assumed and then confirmed self-consistent?" A clean output on an assumed input is `Identified / Derived-conditional on <the named assumption>`, NOT Derived. Sharpen the failure condition to what's *necessary AND sufficient* (Cal: the gate was `e=f`, not the tempting-but-insufficient `O≠e`). Then **localize the last step to a named, specific problem** ("source the gauge embedding") rather than waving it through — that is a far better place than banked-on-an-assumption, and it makes any future "Derived" trustworthy.

**Why / how to apply:** Cal #27 fires HARDEST at the prettiest peak ([[feedback_cal_27_fires_at_peak_convergence]]) — a perfect number IS a peak. Related: commit the checker's half blind ([[feedback_commit_the_checker_half_blind]]) so the fire condition can't drift when the number lands; a "clean form" is a candidate until a mechanism *derives* it ([[feedback_clean_form_is_candidate_not_bank_until_mechanism]]). The both-directions calibration ([[feedback_calibrate_both_directions_not_strict_pessimism]]) still holds: name what IS banked (S1/S3/S4/interior-2 for the muon) and that the favored reading is favored — just don't let the perfect number carry the unsourced identification. The discipline refusing a false PASS at its highest peak is the machine working, not failing.
