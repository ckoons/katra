# © 2025 Casey Koons All rights reserved
#
# Katra Local Configuration Template
# Copy this file to .env.katra.local and customize
# .env.katra.local is git-ignored and should never be committed

# ============================================================================
# Local Overrides
# ============================================================================

# Override log level for local development
# KATRA_LOG_LEVEL=DEBUG

# Override directories for testing
# KATRA_LOG_DIR=/tmp/katra/logs
# KATRA_MEMORY_DIR=/tmp/katra/memory

# ============================================================================
# MCP Server Local Overrides
# ============================================================================

# Override TCP mode for local development
# KATRA_MCP_TCP_MODE=true

# Override TCP port for local testing (avoid conflicts)
# KATRA_MCP_TCP_PORT=3142

# Override bind address (use 0.0.0.0 for network access - INSECURE!)
# KATRA_MCP_TCP_BIND=127.0.0.1

# Override max clients for load testing
# KATRA_MCP_TCP_MAX_CLIENTS=64

# ============================================================================
# API Keys and Secrets (NEVER COMMIT THESE)
# ============================================================================

# Example: Integration with external services (if needed in future)
# EXTERNAL_SERVICE_API_KEY=your-key-here
# ENCRYPTION_KEY=your-encryption-key-here
